{% switch type %}
    {% case 'tooltip' %}
    #{{ id }} + .{{ type }} > .tooltip-inner {
    {% case 'popover' %}
    #{{ id }} + .{{ type }} > .popover-title,
    #{{ id }} + .{{ type }} > .popover-content {
{% endswitch %}
    {{ css | raw }}
}
